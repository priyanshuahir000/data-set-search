# Copy to .env to override defaults. All values are optional.

# Port the API listens on.
PORT=3001

# Source catalog. Remote URL is fetched during ingest and cached locally.
ITEMS_URL=https://media.downshift.app/hiring/founding-engineer/items.json

# Where the built SQLite database lives (relative to the backend folder).
DB_PATH=data/catalog.db
